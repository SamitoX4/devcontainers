version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        MAVEN_VERSION: ${MAVEN_VERSION:-3.9.6}

    volumes:
      - ..:/workspace:cached

    command: sleep infinity

    network_mode: host
