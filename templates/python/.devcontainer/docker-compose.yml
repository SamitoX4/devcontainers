version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        PYTHON_VERSION: ${PYTHON_VERSION:-3.12}

    volumes:
      - ..:/workspace:cached

    command: sleep infinity

    network_mode: host
