version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        PHP_VERSION: ${PHP_VERSION:-8.3}

    volumes:
      - ..:/workspace:cached

    command: sleep infinity

    network_mode: host
